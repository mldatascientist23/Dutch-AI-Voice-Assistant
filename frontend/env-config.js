// Backend API URL Configuration
// This can be set during build time via environment variables
// or manually configured for your deployment

// Set your backend URL here if deploying to Vercel without a build script
// Example: window.BACKEND_API_URL = 'https://your-backend.onrender.com';
window.BACKEND_API_URL = null;

// Alternatively, this will be replaced during build if BACKEND_API_URL env var is set
